{% for actuality in actualities %}
    {% include '@OswisOrgOswisWeb/web/parts/web-actuality-preview.html.twig' with {'loop': loop} %}
{% else %}
    <article class="col-12 actuality-preview">
        {% if page is defined and page > 0 %}
            <div><p>Na tuto stránku nezbyly žádné aktuality.</p></div>
        {% else %}
            <div><p>Nebyly nalezeny žádné aktuality.</p></div>
        {% endif %}
    </article>
{% endfor %}
{% if pagination is defined and pagination %}
    <div class="col-12 actualities-pagination ">
        <div class="container">
            <nav class="row">
                <a class="col-6 text-start">⇐<small class="d-none d-md-inline"> Starší</small></a>
                <a class="col-6 text-end"><small class="d-none d-md-inline">Novější </small>⇒</a>
            </nav>
        </div>
    </div>
{% endif %}